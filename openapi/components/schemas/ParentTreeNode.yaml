type: object
description: Add child params.
properties:
  geoObject:
    $ref: './GeoObject.yaml'
  hierarchyType:
    type: string
  parents:
    type : array
    items :
      $ref: './ParentTreeNode.yaml'
