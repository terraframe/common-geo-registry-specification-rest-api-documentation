type: object
description: Defines a undirected relationship type.
properties:
  code:
    type: string
  label:
    $ref: './LocalizedLabel.yaml'
  description:
    $ref: './LocalizedLabel.yaml'
  organizationCode:
    type: string
