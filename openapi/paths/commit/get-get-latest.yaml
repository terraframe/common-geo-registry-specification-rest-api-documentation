get:
  tags:
    - Publish Graph
  summary: Get latest commit
  description: Get the latest commit of the published graph
  operationId: commit-get-latest
  parameters:
    - name: publishId
      in: query
      description: The uid of the publish graph
      required: true
      schema:
        type: string
      example: f356acf7-63cd-4874-82db-c32dc42d4874  
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/Commit.yaml'

    '401':
      description: Unauthorized
    '403':
      description: Forbidden
    '404':
      description: Not found