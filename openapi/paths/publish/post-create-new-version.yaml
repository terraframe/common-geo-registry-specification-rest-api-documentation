post:
  tags:
    - Publish Graph
  summary: Create a new commit of an existing publish with the events starting from the previous commit
  description: Create a new commit of an existing publish with the events starting from the previous commit.
  operationId: publish-create-new-version
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            uid:
              type: string
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Commit.yaml
    '401':
      description: Unauthorized
    '403':
      description: Forbidden
    '404':
      description: Not found
